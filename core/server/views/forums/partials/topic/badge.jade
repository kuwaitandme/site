html
  body
    // IF posts.user.groups.length
    // BEGIN groups
    // IF ../selected
    // IF ../userTitleEnabled
    a(href='{config.relative_path}/groups/{../slug}')
      small.label.group-label.inline-block(style='background-color: {../labelColor};')
        // IF ../icon
        i.fa(class='{../icon}')
        // ENDIF ../icon
        | {../userTitle}
    // ENDIF ../userTitleEnabled
    // ENDIF ../selected
    // END groups
    // ENDIF posts.user.groups.length
