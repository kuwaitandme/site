doctype html
html(lang= lang, dir= dir, ng-app='App')
  head
    include ./include/head.jade
  body(itemscope, itemtype="http://schema.org/WebSite", id="{{ bodyid }}", ng-class="extraClass", ng-style="bodyStyles", ng-cloak)
    #fb-root

    meta(itemprop="url" content="https://kuwaitandme.com/")

    //- #page-loader(style='position: fixed; width: 100%; height:100%; top:0; left: 0; background:#fff; z-index:99')
    //-   .text(style="font-family: 'Bitter'; color: #000; text-align: center; padding-top: 15%") Loading
    //- #page-progressbar
    header(ng-include="'components/header'")

    #page-loading
      #page-loading-bar

    //- Message container
    ul#messages
    section#page-hero

    //- Here goes the main content of the site
    main(ui-view)
      .robots.hide
        block robots
      block content

    include ./include/footer.jade