.row
  | <% if(obj.hasClassified) { %>
  h2.columns(lang-html='page.cform.edit.title')
  | <% } else { %>
  h2.columns(lang-html='page.cform.begin.title')
  | <% }  %>

  p.columns(lang-html='page.cform.begin.hint1')

  | <% if(obj.isGuest) { %>
  |   <% if(obj.hasClassified) { %>
  p.columns(lang-html='page.cform.edit.guest.hint1')
  |   <% } else { %>
  p.columns(lang-html='page.cform.begin.guest.hint1')
  |   <% }  %>
  p.columns(lang-html='page.cform.begin.guest.hint2')
  | <% } else { %>
  |   <% if(obj.hasClassified) { %>
  p.columns(lang-html='page.cform.edit.hint2')
  |   <% } else { %>
  p.columns(lang-html='page.cform.begin.hint2')
  |   <% } %>
  | <% } %>