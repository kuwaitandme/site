TODO
---------------
* For login; remove captcha, but block 20 incorrect for same session and block for 50 fail on particular account
* CDNify foundation libraries
* trim and add limits to title and other post fields
* add XSS filters
* have notification for guest classifieds
* convert user model to use RESTful API
* find a way to load images properly
* have every view set title accordingly
* have the user cache classifieds he/she has posted
* have user page
* integrate search engine
* make page crawlable
* make sessions expire to avoid bots creating too many sessions!
* redirect the user to the edit page if the user presses back after submitting cl.
* write tests for every page in site
X more versions for cache
X redis to cache jade renders
* Update robots.txt to allow only single classifieds