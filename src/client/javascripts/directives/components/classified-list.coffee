module.exports = ->
  controller = ->

  controller.$inject = []

  link = (scope, element, attributes) ->


  templateUrl: "components/classified-list"
  controller: controller
  link: link
  scope:
    ngImageSuccess: "&"
    ngImageFail: "&"